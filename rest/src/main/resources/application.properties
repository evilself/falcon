server.port=8081
#WebSocket Config
websocket.stomp-endpoint          = ${websocket_stomp_endpoint}
websocket.broker                  = ${websocket_broker}
websocket.app-destination-prefix  = ${websocket_app_destination_prefix}

#Kafka Config
kafka.server                      = ${spring.kafka.bootstrap-servers}
kafka.persister-topic             = ${PERSISTER_TOPIC}
kafka.viewer-topic                = ${VIEWER_TOPIC}
kafka.group-id                    = ${GROUP_ID_CONFIG}
kafka.enable-аuto-commit          = ${ENABLE_AUTO_COMMIT_CONFIG}
kafka.auto-commit-interval-ms     = ${AUTO_COMMIT_INTERVAL_MS_CONFIG}
kafka.session-timeout-ms          = ${SESSION_TIMEOUT_MS_CONFIG}
kafka.auto-offset                 = ${AUTO_OFFSET_RESET_CONFIG}
kafka.concurrency                 = ${CONCURRENCY}
kafka.poll-timeout                = ${POLL_TIMEOUT}
kafka.topics                      = ${SCORE_TOPICS}